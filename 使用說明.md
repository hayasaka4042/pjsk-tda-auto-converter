# PJSK → TDA 自動轉換器

適用環境：Blender 4.2.22，以及目前這支 `Tda式初音ミク` 骨架。

## 安裝

1. Blender：`Edit → Preferences → Add-ons → Install from Disk`。
2. 選擇 `PJSK_TDA_AutoConverter.zip`。
3. 啟用 `PJSK to TDA Auto Converter`。

## 最快使用方式

1. 開啟來源 `.blend` 的安全副本，匯入 TDA 模型與動作。
2. 打開 3D View 右側欄的 `PJSK→TDA` 分頁。
3. 按 `轉換所選 Action`，等待 Blender 顯示 PASS，再另存新 `.blend`。

場景只有一個相容骨架時，插件會自動使用它；有多個骨架時才需要手動選取。沒有在面板指定 Action 時，會使用目標骨架目前正在播放的 Action。輸出名稱可以留空。

工具會建立：

- `<輸出名稱>`：可直接給 TDA 使用的新 Action。
- `<輸出名稱>_LowerBody_WORLD_BAKE`：來源骨架的 LowerBody 世界／armature-space 烘焙紀錄。
- `<輸出名稱>_validation.txt`：Blender Text Editor 裡的驗證結果。

固定保護：

- 原輸入 Action 不修改。
- 不直接把來源 Action 指派給 TDA 當成結果。
- 不新增 IK。
- 自動關閉左右腿與腳尖 IK，並逐幀確認沒有重新啟用。
- 不建立 scale curves。
- TDA rest matrix、父子關係或既有 constraint 不符時會停止。
- 輸出 Action 名稱已存在時會停止，不會覆蓋。
- 任何步驟失敗時會恢復原 Action 與原 IK 狀態，不留下半成品 Action。

這是此 TDA 模型專用工具；換其他模型時不會強行轉換。
